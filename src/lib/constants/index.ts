export const APP_NAME = "St-Cntrers";

export const ROUTES = {
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
  DASHBOARD: "/dashboard",
} as const;

export const API_ROUTES = {
  AUTH: "/api/auth",
  USERS: "/api/users",
} as const;
